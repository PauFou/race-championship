<div class="form-container">
  <h2>Crée ton championnat 🚀</h2>
  <%= simple_form_for(@championship) do |f| %>
    <div class="form-inputs">
      <%= f.input :title, placeholder: 'Votre titre...' %>
      <%= f.input :description, placeholder: 'Votre championnat détaillé...' %>
      <%= f.submit "Créer votre championnat", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
